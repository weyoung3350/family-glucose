# B02 · 数据模型与建表

## 目标
定义 `families`、`users`、`glucose_records` 三张 SQLModel 表，并提供 `init_db.py` 一键建表脚本。

## 前置依赖
B01

## 上下文
- 概要设计 §5（数据模型）、§5.4（时段枚举）

## 输入
- `backend/app/config.py`（B01 输出）
- 概要设计 §5 的字段定义

## 输出

```
backend/
├── app/
│   ├── database.py
│   ├── models.py
│   └── enums.py
└── scripts/
    └── init_db.py
```

## 详细需求

### app/enums.py
导出 Python 枚举：

```python
class Period(str, Enum):
    fasting = "fasting"
    before_breakfast = "before_breakfast"
    after_breakfast = "after_breakfast"
    before_lunch = "before_lunch"
    after_lunch = "after_lunch"
    before_dinner = "before_dinner"
    after_dinner = "after_dinner"
    bedtime = "bedtime"

PERIOD_LABELS = {
    Period.fasting: "空腹",
    Period.before_breakfast: "早餐前",
    Period.after_breakfast: "早餐后",
    Period.before_lunch: "午餐前",
    Period.after_lunch: "午餐后",
    Period.before_dinner: "晚餐前",
    Period.after_dinner: "晚餐后",
    Period.bedtime: "睡前",
}

class Role(str, Enum):
    creator = "creator"
    member = "member"

class Source(str, Enum):
    manual = "manual"
    ai = "ai"

class GradeLevel(str, Enum):
    low = "low"
    ideal = "ideal"
    ok = "ok"
    high = "high"
    vhigh = "vhigh"

GRADE_LABELS = {
    GradeLevel.low: "偏低",
    GradeLevel.ideal: "理想",
    GradeLevel.ok: "一般",
    GradeLevel.high: "偏高",
    GradeLevel.vhigh: "过高",
}
GRADE_COLORS = {
    GradeLevel.low: "#4DA3FF",
    GradeLevel.ideal: "#52C41A",
    GradeLevel.ok: "#FAAD14",
    GradeLevel.high: "#FA8C16",
    GradeLevel.vhigh: "#F5222D",
}
```

### app/database.py
- `engine = create_engine(settings.DATABASE_URL, echo=settings.APP_ENV=="development", connect_args={"check_same_thread": False})`
- `def get_session() -> Generator[Session, None, None]:` yield `Session(engine)` 用于 FastAPI 依赖注入
- `def init_db() -> None:` 调用 `SQLModel.metadata.create_all(engine)`

### app/models.py
按概要设计 §5 字段定义三张 SQLModel 表：

```python
class Family(SQLModel, table=True):
    __tablename__ = "families"
    id: Optional[int] = Field(default=None, primary_key=True)
    name: str = Field(index=True, unique=True)
    invite_code: str = Field(index=True, unique=True)
    creator_id: int = Field(foreign_key="users.id")
    fasting_low: float = 4.4
    fasting_high: float = 6.1
    postprandial_low: float = 4.4
    postprandial_high: float = 7.8
    critical_low: float = 3.9
    critical_high: float = 13.9
    created_at: datetime = Field(default_factory=datetime.utcnow)
    updated_at: datetime = Field(default_factory=datetime.utcnow)

class User(SQLModel, table=True):
    __tablename__ = "users"
    id: Optional[int] = Field(default=None, primary_key=True)
    openid: str = Field(index=True, unique=True)
    nickname: str = ""
    avatar_url: Optional[str] = None
    family_id: Optional[int] = Field(default=None, foreign_key="families.id", index=True)
    role: Optional[str] = None  # creator | member
    joined_at: Optional[datetime] = None
    created_at: datetime = Field(default_factory=datetime.utcnow)

class GlucoseRecord(SQLModel, table=True):
    __tablename__ = "glucose_records"
    id: Optional[int] = Field(default=None, primary_key=True)
    family_id: int = Field(foreign_key="families.id", index=True)
    recorder_id: int = Field(foreign_key="users.id", index=True)
    value: float
    period: str  # 时段枚举值
    measured_at: datetime = Field(index=True)
    note: Optional[str] = None
    source: str = "manual"
    created_at: datetime = Field(default_factory=datetime.utcnow)
    updated_at: datetime = Field(default_factory=datetime.utcnow)
```

注意：
- `Family.creator_id` 与 `User.id` 之间是循环外键，建表时不要加 `nullable=False` 在 SQLite 层硬约束（创建家庭时先建 Family 再建 User 关联），ORM 层校验即可
- 所有 datetime 用 UTC 存（`datetime.utcnow`），前端展示时按本地时区转

### scripts/init_db.py
```python
"""一键建表。已存在的表不会重建。"""
from app.database import init_db
init_db()
print("ok")
```

## 验收标准

- [ ] `python scripts/init_db.py` 创建 `data/glucose.db` 且包含 3 张表
- [ ] `sqlite3 data/glucose.db ".schema"` 显示三张表的 DDL，外键约束正确
- [ ] `sqlite3 data/glucose.db "PRAGMA index_list(families);"` 看到 `name`、`invite_code` 唯一索引
- [ ] Python 中 `from app.models import Family, User, GlucoseRecord` 无错
- [ ] `from app.enums import Period, GRADE_COLORS` 能正确导入并访问值

## 不在范围内
- 任何业务逻辑（CRUD 由 B04/B05 处理）
- 数据迁移工具（首版直接 create_all 即可）
- 测试数据 seed
