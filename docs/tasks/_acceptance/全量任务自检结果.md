# B04-F10/T01 全量任务自检结果

> 自检时间：2026-04-30 20:09:35 CST  
> 自检人：Codex  
> 范围：B04-B10、F01-F10、T01  
> 结论：本地可自动化验证项已完成；真实微信开发者工具、生产服务器、三微信账号端到端验收未执行，T01 不能判定为全通过。

## 文件清单

### 后端

- 新增/修改：`backend/app/main.py`
- 新增/修改：`backend/pyproject.toml`
- 新增：`backend/app/routers/families.py`
- 新增：`backend/app/routers/records.py`
- 新增：`backend/app/routers/analytics.py`
- 新增：`backend/app/routers/export.py`
- 新增：`backend/app/routers/ai.py`
- 新增：`backend/app/schemas/family.py`
- 新增：`backend/app/schemas/record.py`
- 新增：`backend/app/schemas/analytics.py`
- 新增：`backend/app/schemas/standards.py`
- 新增：`backend/app/schemas/ai.py`
- 新增：`backend/app/services/grading.py`
- 新增：`backend/app/services/time_utils.py`
- 新增：`backend/app/services/analytics.py`
- 新增：`backend/app/services/csv_export.py`
- 新增：`backend/app/services/ai_parser.py`
- 新增：`backend/deploy/README.md`
- 新增：`backend/deploy/nginx.conf.example`
- 新增：`backend/deploy/glucose-api.service.example`
- 新增：`backend/deploy/backup.sh`
- 新增：`backend/deploy/logrotate.conf.example`

### 小程序

- 新增/修改：`miniprogram/app.js`
- 新增/修改：`miniprogram/app.json`
- 新增/修改：`miniprogram/app.wxss`
- 新增/修改：`miniprogram/project.config.json`
- 新增/修改：`miniprogram/sitemap.json`
- 新增：`miniprogram/utils/api.js`
- 新增：`miniprogram/utils/color.js`
- 新增：`miniprogram/utils/const.js`
- 新增：`miniprogram/utils/period.js`
- 新增：`miniprogram/utils/share.js`
- 新增：`miniprogram/utils/time.js`
- 新增：`miniprogram/components/record-card/*`
- 新增：`miniprogram/components/glucose-chip/*`
- 新增：`miniprogram/components/period-picker/*`
- 新增/修改：`miniprogram/pages/index/*`
- 新增/修改：`miniprogram/pages/add/*`
- 新增/修改：`miniprogram/pages/ai-add/*`
- 新增/修改：`miniprogram/pages/detail/*`
- 新增/修改：`miniprogram/pages/analytics/*`
- 新增/修改：`miniprogram/pages/me/*`
- 新增/修改：`miniprogram/pages/join/*`
- 新增/修改：`miniprogram/pages/family/*`
- 新增/修改：`miniprogram/pages/standards/*`
- 新增/修改：`miniprogram/pages/export/*`
- 新增/修改：`miniprogram/pages/about/*`
- 新增：`miniprogram/images/tab-home.png`
- 新增：`miniprogram/images/tab-home-active.png`
- 新增：`miniprogram/images/tab-analytics.png`
- 新增：`miniprogram/images/tab-analytics-active.png`
- 新增：`miniprogram/images/tab-me.png`
- 新增：`miniprogram/images/tab-me-active.png`
- 新增：`miniprogram/images/mic.png`

### 验收记录

- 新增：`docs/tasks/_acceptance/全量任务自检结果.md`
- 新增：`docs/tasks/_acceptance/T01-端到端验收报告.md`

## 验证命令

| 命令 | 结果 | 证据 |
| --- | --- | --- |
| `APP_ENV=development JWT_SECRET=test-secret LLM_PROVIDER=none /tmp/glucose-full-venv/bin/python -m compileall app` | 通过 | Python 文件全部编译成功。 |
| `find miniprogram -name '*.js' -print0 \| xargs -0 -n1 node --check` | 通过 | 所有小程序 JS 语法检查通过。 |
| `node` 调用 `utils/period.js` 与 `utils/time.js` | 通过 | `periodLabel("after_breakfast") == "早餐后"`，4 个 5 分钟取整用例通过。 |
| FastAPI TestClient 集成脚本 | 通过 | 78 个后端断言通过，覆盖家庭、记录、分析、标准、CSV、AI。 |
| FastAPI PATCH 取整脚本 | 通过 | `PATCH /records/{id}` 提交 `09:23` 后返回 `09:25`。 |
| `find miniprogram/images -name '*.png' -exec ls -lh {} +` | 通过 | 7 张图片均约 201-202B，小于 30KB。 |

## 已知文档冲突

| 编号 | 冲突 | 本次处理 |
| --- | --- | --- |
| B04-验收项 3 | 验收项写“用户 A 再次创建同名咱家 → 409”，但详细需求写“用户当前 family_id 必须为空，否则 400 ERR_USER_ALREADY_IN_FAMILY”。 | 实现按详细需求：已在家庭内再创建返回 400；同名校验对未入家庭用户返回 409。 |
| B05-验收项 3 | 验收项写 `value=11.1, period=fasting` 应为 `high/#FA8C16`，但概要设计 §9.1 算法规定 `< critical_high * 0.85` 为 `ok/#FAAD14`，11.1 < 11.815。 | 实现按概要设计：11.1 空腹返回 `ok/#FAAD14`。 |

## B04 家庭管理 API

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 用户 A 创建家庭“咱家”，返回 invite_code 可见 | 通过 | TestClient：`POST /api/v1/families` 返回 creator 与 invite_code。 |
| 用户 B 用 invite_code 加入，B 在 GET /me 中 role=member、invite_code=null | 通过 | TestClient：加入返回 `role_of_me=member`，`invite_code=null`。 |
| 用户 A 再次创建同名“咱家” → 409 | 文档冲突 | 实现按详细需求先拦截 `family_id`，已入家庭用户返回 400；未入家庭用户创建同名返回 409。 |
| 用户 B 调 PATCH /me 改家庭名 → 403 ERR_PERMISSION_DENIED | 通过 | TestClient：成员 PATCH 返回 403。 |
| 用户 A 删除 B 后 B 的 GET /me → 403 ERR_NOT_IN_FAMILY | 通过 | TestClient：移除成员后 GET 返回 403。 |
| 用户 B 加入新家庭后 POST /me/leave 成功 | 通过 | TestClient：B 被移除后加入“二家”，退出返回 204。 |
| 创建者 POST /me/leave → 400 ERR_CREATOR_CANNOT_LEAVE | 通过 | TestClient：creator leave 返回 400。 |
| 创建者 DELETE /me 解散家庭后 member GET /me → 403 | 通过 | TestClient：解散后儿子 GET 返回 403。 |
| 错误邀请码“AAAAAA” → 404 ERR_INVITE_CODE_INVALID | 通过 | TestClient：返回 404。 |
| 覆盖以上路径至少 12 个测试用例 | 通过 | 临时验收脚本共 78 个后端断言；未新增 pytest 文件，因为任务输出未列出 tests。 |

## B05 血糖记录 CRUD 与分级

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 分级边界覆盖 3.8/3.9/4.4/6.1/6.2/7.8/13.8/13.9/14.0 | 通过 | 直接调用 `grade`，结果符合概要设计 §9.1。 |
| 5 分钟取整 09:23/09:22/09:58/09:00 | 通过 | Python 与前端 JS 取整用例均通过。 |
| 创建 `11.1 fasting` 后 status.level=high | 文档冲突 | 按概要设计 §9.1 返回 `ok/#FAAD14`；任务验收项写 high。 |
| 创建 `8.2 after_breakfast` → status.level=ok | 通过 | `grade(8.2, after_breakfast)` 返回 `ok`。 |
| GET 列表按 measured_at desc 排序 | 通过 | TestClient：`/records?from=&to=&size=50` 首尾时间倒序。 |
| 用户 A 不能 PATCH 用户 B 记录 → 403 | 通过 | TestClient：成员修改记录人记录返回 `ERR_NOT_RECORDER`。 |
| measured_at=09:23 创建后 DB 实际存 09:25 | 通过 | TestClient：创建返回 `measured_at=09:25:00`。 |
| 跨家庭访问记录 ID → 404 ERR_RECORD_NOT_FOUND | 通过 | TestClient：A 家访问 B 家记录返回 404。 |

## B06 分析 API

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 连续 10 天每天 2 条，matrix days 长度 10 且每天 8 个 period key | 通过 | TestClient：`/analytics/matrix` 返回 10 天、每格 8 key。 |
| 同一天同时段重测两次，矩阵显示最新一条 | 通过 | 2026-04-01 fasting 两条记录，矩阵显示 08:00 的 9.9。 |
| `/chart?period=fasting` 仅返回空腹记录 | 通过 | TestClient：所有 points 的 period 均为 fasting。 |
| `/report` abnormal_days 等于人工统计值 | 部分通过 | 接口返回整数并参与集成流；未在报告中保留完整人工统计表。 |
| from > to → 400 ERR_INVALID_RANGE | 通过 | TestClient：返回 400。 |
| 400 天范围 → 400 ERR_RANGE_TOO_LARGE | 通过 | TestClient：返回 400。 |
| 跨家庭隔离 | 通过 | 隔壁家 chart 仅统计隔壁家 1 条记录。 |

## B07 自定义血糖标准

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 新建家庭 GET /standards 返回默认值 | 通过 | 返回 4.4/6.1/4.4/7.8/3.9/13.9。 |
| member 调 PATCH → 403 ERR_PERMISSION_DENIED | 通过 | TestClient：成员 PATCH 返回 403。 |
| creator 改 fasting_high=10 → 200 OK | 通过 | TestClient：返回 `fasting_high=10.0`。 |
| creator 改 fasting_low=5, fasting_high=4 → 400 | 通过 | 返回 `ERR_STANDARDS_INVALID`。 |
| creator 改 critical_low=5, fasting_low=4 → 400 | 通过 | 返回 `ERR_STANDARDS_INVALID`。 |
| 改完 standards 后 value=8 fasting 按新阈值计算 | 通过 | 阈值改为 10 后，8.0 fasting 返回 ideal。 |

## B08 CSV 导出

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| Excel 打开中文不乱码 | 部分通过 | 已验证 BOM 与 UTF-8-SIG；未实际打开 Excel。 |
| 第一行中文表头 | 通过 | CSV 第一行为 `日期,时间,时段,时段名,血糖值,单位,状态,记录人,备注`。 |
| 导出条数与 records 查询累计一致 | 通过 | CSV 行数不少于当前查询 total，换行备注会增加物理行。 |
| 备注逗号或换行正确转义 | 通过 | CSV 中出现 `"早餐,备注\n换行"`。 |
| 跨家庭 CSV 不含 B 家数据 | 通过 | CSV 内容不含隔壁家记录。 |
| 1000+ 条范围导出不会内存溢出 | 通过 | 直接插入 1001 条后导出成功，响应约 69KB。 |

## B09 AI 一句话解析

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 完整样例解析 value/period/time/note | 通过 | 规则解析返回 11.1、fasting、09:20、吃了二甲双胍。 |
| `9:20 血糖 11.1` 推断 after_breakfast | 通过 | 返回 `period=after_breakfast` 且 `period_inferred=true`。 |
| `刚才血糖 7.8` 推断当前时间和时段 | 通过 | 固定 now=09:23 时返回 09:25 与 after_breakfast。 |
| `饭后两小时 8.0` 推断最近餐后 | 通过 | 固定 now=09:23 时返回 after_breakfast。 |
| `11.1` → period=null, missing=['period'] | 通过 | 已修正：endpoint 返回 `missing=["period"]`。 |
| 空字符串 → 400 | 通过 | endpoint 返回 400 `ERR_HTTP`。 |
| LLM 异常 fallback 规则解析器 | 通过 | `LLM_PROVIDER=none` 本地路径等价验证 fallback；未调用真实异常 HTTP。 |
| 真实 DeepSeek key 集成 | 未验证 | 当前环境未提供 DeepSeek key。 |

## B10 生产部署

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| Ubuntu 22.04 按 README 部署后 health 200 | 未验证 | 未提供测试服务器和域名。 |
| systemd active running | 未验证 | 未连接服务器。 |
| 重启服务器后自启 | 未验证 | 未连接服务器。 |
| 写记录后停服重启仍在 | 未验证 | 未连接服务器。 |
| backup.sh 生成备份 | 静态通过 | `backend/deploy/backup.sh` 已提供并设为可执行；未在目标服务器执行。 |
| logrotate 强制轮转 | 静态通过 | `backend/deploy/logrotate.conf.example` 已提供；未在目标服务器执行。 |

## F01 小程序项目骨架

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 微信开发者工具打开无编译错误 | 未验证 | 当前环境未运行微信开发者工具。 |
| TabBar 三 tab 可切换，激活态橙色 | 静态通过 | `app.json` 已配置三 tab 与橙色 selectedColor。 |
| 首页/分析/我的三页可见 | 静态通过 | 三个页面文件已存在并接入 TabBar。 |
| 顶部导航栏显示“家有糖人” | 静态通过 | `app.json` window 已配置。 |
| `getApp().globalData` 可访问 | 静态通过 | `app.js` 定义 globalData。 |
| images 目录下 6 张图标存在且每张小于 30KB | 通过 | 7 张 png 均约 201-202B。 |

## F02 工具与 API 封装

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| `periodLabel("after_breakfast")` 返回“早餐后” | 通过 | Node 断言通过。 |
| 前端 roundTo5Min 四个用例 | 通过 | Node 断言通过。 |
| `api.listRecords` 请求和 Authorization header | 静态通过 | `api.js` 已封装 query 与 Bearer token；未在微信网络面板验证。 |
| 模拟 401 自动 re-login 并重试一次 | 静态通过 | `api.js` 401 分支已实现；未运行 wx mock。 |
| `api.csvUrl({from,to})` 含 query 与 token | 静态通过 | `api.js` 使用 `appendQuery` 拼接 token。 |

## F03 加入家庭页 P09

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| family=null 自动跳 join | 静态通过 | `app.js` onLaunch/onShow 逻辑存在。 |
| 正确邀请码加入后跳首页并填 globalData.family | 静态通过 | `join.js` 调 `api.joinFamily` 后写 globalData 并 reLaunch。 |
| 错误邀请码 toast | 静态通过 | `join.js` catch 分支显示 toast。 |
| 创建新家庭 modal 与跳首页 | 静态通过 | `join.wxml/js` 已实现创建弹层。 |
| 创建同名家庭 modal 不关并显示红字 | 静态通过 | `join.js` 保存 error 到弹层。 |
| 分享路径含 code 自动预填 | 静态通过 | `join.js onLoad` 读取 code。 |
| 输入自动大写 | 静态通过 | `join.js onCodeInput` 转 uppercase。 |
| 输入限制 6 位 | 静态通过 | `join.js` slice(0, 6)。 |
| 视觉差异 ≤ 10% | 未验证 | 未进行截图对比。 |

## F04 首页 P01

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 首页显示全家记录、倒序、按日期分组 | 静态通过 | `index.js` 拉取 records 并 group by date。 |
| 今日已测 2 | 静态通过 | `index.js` 计算 todayCount。 |
| tap 跳详情 | 静态通过 | `record-card` bindtap 到详情。 |
| FAB 与麦克风按钮可见 | 静态通过 | `index.wxml/wxss` 已实现。 |
| 麦克风跳 ai-add | 静态通过 | `goAiAdd` 已实现。 |
| 主按钮跳 add | 静态通过 | `goAdd` 已实现。 |
| 触底加载更多 | 静态通过 | `onReachBottom` 已实现。 |
| 空数据态 | 静态通过 | `index.wxml` 有 empty 视图。 |
| 视觉差异 ≤ 10% | 未验证 | 未进行截图对比。 |

## F05 手动新增 P02 与详情 P03

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 新增 7.2 早餐后保存后首页可见 | 静态通过 | `add.js` 调 `api.createRecord` 后 navigateBack；未在 DevTools 执行。 |
| 时间 picker 分钟列 00-55 步进 5 | 静态通过 | `add.js` minutes 数组为 12 项。 |
| 输入 0 或 51 提示错误，不写库 | 静态通过 | `add.js` 前端校验 `>0 && <50`。 |
| 编辑模式预填并保存更新详情 | 静态通过 | `add.js` 支持 id 拉详情与 PATCH。 |
| 顶部 AI 一句话跳 P02b | 静态通过 | `add.wxml/js` 已实现。 |
| 详情显示大数字、状态胶囊、meta | 静态通过 | `detail.wxml` 已实现。 |
| AI 录入显示 AI 标签 | 静态通过 | `detail.wxml` 根据 source 显示。 |
| 自己记录显示编辑/删除，他人无 | 静态通过 | `detail.js` 计算 `canEdit`。 |
| 删除后回列表消失 | 静态通过 | `detail.js` 调 delete 后 navigateBack。 |
| 编辑后回详情更新 | 静态通过 | `detail.js onShow` 重载详情。 |
| 视觉差异 ≤ 10% | 未验证 | 未进行截图对比。 |

## F06 AI 快记 P02b

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 完整样例 800ms 后解析卡片出现 | 静态通过 | `ai-add.js` debounce 800ms；后端样例解析通过。 |
| “血糖 11.1”缺时段且保存置灰 | 静态通过 | `ai-add.js` 使用 missing/canSave；后端 `11.1` 返回 missing period。 |
| 修改字段后保存请求 body 使用修改值 | 静态通过 | `ai-add.js` save 使用页面 data parsed。 |
| `period_inferred=true` 显示“推断” | 静态通过 | `ai-add.wxml` 显示推断文案。 |
| 重新输入清空 textarea 与解析结果 | 静态通过 | `reset` 已实现。 |
| 网络异常红字提示且不阻断编辑 | 静态通过 | catch 后设置 error。 |
| 分段切换手动录入跳 P02 | 静态通过 | `goManual` 已实现。 |
| 视觉差异 ≤ 10% | 未验证 | 未进行截图对比。 |

## F07 分析 Tab P04/P05/P06

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 最近 7 天矩阵 7 行 8 列 | 静态通过 | `analytics.js/wxml` 已按 periods 渲染；后端 matrix 10 天 8 key 通过。 |
| 有记录格子颜色数值，无记录灰底 “-” | 静态通过 | `analytics.wxml` 已按 status.color/空值渲染。 |
| 点格子跳详情 | 静态通过 | `onCellTap` 已实现。 |
| 折线图能渲染 | 静态通过 | 已补 `canvas type="2d"` 折线绘制；未在移动端渲染验证。 |
| 空腹筛选仅 fasting | 静态通过 | 前端筛选与后端筛选均存在；后端已验证。 |
| 统计卡数字与后端 chart.stats 一致 | 静态通过 | 直接展示后端 stats。 |
| 分布柱条 5 段宽度 | 静态通过 | 已按 low/ideal/ok/high/vhigh 统计生成 5 段宽度。 |
| hero 平均值 1 位小数 | 静态通过 | 后端 report avg 为 1 位；前端直接展示。 |
| 异常天数与后端一致 | 静态通过 | 前端直接展示后端 summary。 |
| 8 时段全部出现 | 静态通过 | 后端 by_period 始终 8 项；前端循环展示。 |
| 视觉差异 ≤ 15% | 未验证 | 未进行截图对比。 |

## F08 我的与关于

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 我的页头像、昵称、家庭名+角色 | 静态通过 | `me.wxml/js` 已实现。 |
| 4 个菜单项跳转 | 静态通过 | `goFamily/goStandards/goExport/goAbout` 已实现。 |
| 角色显示成员/管理员 | 静态通过 | `me.js` 计算 roleLabel。 |
| 头像首字非空，昵称空显示“我” | 静态通过 | `me.js` 计算 avatarText。 |
| P07 视觉差异 ≤ 10% | 未验证 | 未截图。 |
| 关于页 logo、名称、版本号居中 | 静态通过 | `about.wxml/wxss` 已实现。 |
| 关于页 3 个菜单项可点 | 静态通过 | `about.wxml/js` 已实现。 |
| 底部声明可见 | 静态通过 | `about.wxml` 已实现。 |
| P12 视觉差异 ≤ 10% | 未验证 | 未截图。 |

## F09 家庭管理与标准

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 管理员看邀请码和按钮，成员不可见 | 静态通过 | `family.js/wxml` 基于 `isCreator` 控制。 |
| 复制按钮写入剪贴板 | 静态通过 | `copyInvite` 调 `wx.setClipboardData`。 |
| 分享按钮 open-type=share，标题带家庭名 | 静态通过 | `family.wxml` 使用 `open-type="share"`，`onShareAppMessage` 带家庭名。 |
| 管理员移除成员后列表减少，成员重开进入 P09 | 静态通过 | 前端 remove 后 reload，app/family 页面有家庭缺失跳转；后端移除验证通过。 |
| 管理员解散家庭后跳 P09 | 静态通过 | `dissolve` 成功后 reLaunch join。 |
| 成员退出家庭后跳 P09 | 静态通过 | `leave` 成功后 reLaunch join。 |
| P08 视觉差异 ≤ 10% | 未验证 | 未截图。 |
| 默认标准显示正确 | 静态/后端通过 | 后端默认值验证通过，前端加载展示。 |
| 管理员改 fasting_high=6.5 保存成功 | 静态通过 | 前端提交 PATCH；后端 creator PATCH 验证通过。 |
| 非法标准 toast 错误，不保存 | 静态通过 | 前端 catch toast；后端非法标准验证通过。 |
| 成员页面数字不可点，无保存按钮 | 静态通过 | `standards.wxml` 基于 `isCreator` 禁用/隐藏。 |
| P10 视觉差异 ≤ 10% | 未验证 | 未截图。 |

## F10 数据导出 P11

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 默认最近 30 天预览 total 正确 | 静态通过 | `export.js` 默认 days=30 并调用 listRecords；未在 DevTools 执行。 |
| 切换 7/90 天 total 正确变化 | 静态通过 | `selectRange` 重新 loadPreview。 |
| 点击生成并分享 CSV 弹微信分享面板 | 静态通过 | 使用 `wx.downloadFile` + `wx.shareFileMessage`；未实机验证。 |
| 接收方 Excel 打开中文无乱码 | 部分通过 | 后端 BOM 验证通过；未实际微信传输与 Excel 打开。 |
| 接收方第一行中文表头 | 通过 | 后端 CSV 表头验证通过。 |
| 空家庭仍下载空 CSV 且 toast “没有记录” | 静态通过 | `export.js` total=0 分支 toast；后端会输出仅表头。 |
| 视觉差异 ≤ 10% | 未验证 | 未截图。 |

## T01 端到端联调

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 全部 10 个 TC 通过 | 未通过 | 未提供生产服务器、微信 AppID、微信开发者工具运行结果、3 个真实微信账号。 |
| 输出验收报告文档 | 通过 | 已输出 `docs/tasks/_acceptance/T01-端到端验收报告.md`。 |
| 若有 P0/P1 缺陷，明确指向任务 | 通过 | 本地自动化未发现 P0/P1 代码缺陷；已标注文档冲突和未验证项。 |

## 问题清单

| 等级 | 任务 | 问题 | 建议 |
| --- | --- | --- | --- |
| P2 | F07 | 图表已改为 canvas 折线和 5 段分布条，但未在微信开发者工具做渲染实测。 | 在真实小程序环境截图验证 P04/P05/P06。 |
| P2 | B04 | 验收项 3 与详细需求冲突。 | 产品/任务文档明确已入家庭再创建同名时应优先返回 400 还是 409。 |
| P2 | B05 | 11.1 空腹分级验收项与概要设计算法冲突。 | 产品/技术文档统一高/一般的阈值。 |
| P2 | T01 | 真实端到端环境缺失。 | 准备 HTTPS 后端、AppID、微信开发者工具和 3 个微信账号后执行实机验收。 |
